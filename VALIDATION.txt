DDS Companion 0.4.0 — Local Validation
======================================

Date: 2026-09-14
Status: BUILT / LOCAL TESTING

Compile
-------
PASS — python -m compileall dds_companion

Automated suite
---------------
PASS — 21/21 tests

Regression coverage retained:
- importer / dedup / changed snapshot / bad JSON isolation
- thread parent relationship
- Activity persistence + subscriber isolation
- Health core + stale watcher
- Stats session baseline
- schema v1 → v2 additive upgrade
- watcher settle / new / changed / delete / failure recovery
- bad observer isolation
- clean watcher state/heartbeat behavior

New 0.4.0 coverage:
1. LibraryService reconstructs Guild → Channel → Thread and message counts.
2. GuiRuntime opens a real SQLite archive, imports DDS capture data, emits a
   RUNNING snapshot and stops cleanly on request.
3. GuiRuntime detects a changed capture and emits capture_imported with the
   expected +1 new / 1 refreshed message counts.

Qt render validation
--------------------
NOT RUN in build environment: PySide6 is not installed there.

This is an explicit validation boundary, not a silent PASS. The GUI source was
syntax-compiled and its backend/threading integration is tested without Qt.
Native Windows visual/render/folder-action validation is required before VERIFIED.
