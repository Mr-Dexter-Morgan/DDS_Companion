DDS Companion 0.5.7 — короткий Windows live-check

1. Библиотека / hover
   - Щёлкни конкретный канал/тему.
   - Уведи курсор вправо на просмотр сообщений.
   - Верни курсор влево и проведи через соседние строки БЕЗ клика.
   PASS: соседние строки не выглядят выбранными; постоянная подсветка остаётся только на реально выбранной строке; preview не меняется.

2. Библиотека / геометрия
   - Проверь левый заголовок «Раздел» и начало названий строк.
   PASS: ничего не сдвинуто влево старым horizontal-scroll offset; горизонтальной полосы прокрутки нет.

3. Выгрузка
   - У каждой строки доступны только два варианта: «Не выгружать» / «Выгружать».
   - Переключи пару строк и дождись фонового refresh.
   PASS: выбор сохраняется; «По умолчанию» в UI отсутствует; геометрия комбобоксов не прыгает.

4. Сообщения / форматирование
   - Открой старые сообщения с жирным/курсивом и, если есть, custom emoji.
   PASS: Markdown остаётся читаемым; Discord custom emoji не превращаются в пустой object glyph, а показываются как :имя:.

5. Активность
   - Открой страницу «Активность».
   PASS: русские названия событий помещаются заметно лучше; heartbeat-spam не вернулся.

6. Диагностика
   - Настройки -> Диагностика.
   PASS: подсказки читаются по-русски; «Контракт захвата» показывает `capture-v2 · plugin-heartbeat-v1`, а не `capture —`.

7. Регрессия уже работающих частей
   - Быстро проверь Главную и Статус -> Жизненный цикл медиа.
   PASS: 85/186/101 (или актуальные значения твоей базы) остаются логически согласованными; списки/скролл не дёргаются.

8. Запуск из BAT
   - Странности taskbar/Python identity сейчас НЕ являются блокером 0.5.7: это отдельная задача 0.6.0 EXE.
